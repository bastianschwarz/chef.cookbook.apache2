---
driver:
  name: vagrant

provisioner:
  name: chef_solo

verifier:
  name: inspec

platforms:
  - name: debian-9.4

suites:
  - name: default
    run_list:
      - recipe[codenamephp_apache2::default]
    verifier:
      inspec_tests:
        - test/smoke/default
    attributes:
